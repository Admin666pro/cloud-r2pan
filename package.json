{
  "name": "crystal-drive",
  "version": "1.0.0",
  "private": true,
  "description": "Crystal Drive - iOS 26 液态玻璃风格网盘分享系统 (Cloudflare Workers + R2 + D1)",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit && wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
